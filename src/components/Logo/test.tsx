import { render, screen } from '@testing-library/react'

import Logo from '.'

describe('<Logo />', () => {
  it('should render a white label by default', () => {
    //1 - renderizar o componente com o 'render'
    //2- selecionar o elemento a ser testacom com o 'screen' + (queries) - getByLabel...
    //3- analisar o comportamento esperado com o 'expect'
  })
})
